/node_modules
.env

# Logs locales del proyecto (Punto 3 y 4 — fallback file)
logs/

# Reportes y artefactos de OWASP ZAP (no deben subirse al repo)
zap-reports/
zap-*.html
zap-*.json
zap-*.xml
zap-*.md
*.zap.session
*.zap.session.lck
*.zap.session.properties
